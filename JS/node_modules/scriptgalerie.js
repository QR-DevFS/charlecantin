$(document).ready(function () {
  $(".dropdown button").on("shown.bs.dropdown", function () {}).on;
  $(".dropdown-menu a").on("click", function () {
    let category = $(this).attr("data-category");
    $(".pictures").hide();
    if ($("section").hasClass("dispo")) {
      $(".dispo").append($("." + category).show());
      console.log("hello");
    } else {
      addElement();

      $(".dispo").append($("." + category).show());
      console.log("try");
    }
  });
});

$(".dispo section").hide();

function addElement() {
  let ok = document.createElement("section");
  ok.classList.add("dispo");
  var currentDiv = document.getElementById("div1");
  currentDiv.appendChild(ok);
}
